// Prerender every route to static HTML — this is what makes a real
// build/index.html that GitHub Pages can serve directly, with no SPA
// fallback or 404 redirect needed.
export const prerender = true;
export const trailingSlash = 'always';
